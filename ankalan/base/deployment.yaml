apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
  labels:
    appgroup: ankalan
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
    spec:
      containers:
      - name: app
        image: nginx:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_APP_NAME
          value: "spring-boot-k8s"
        - name: SERVER_PORT
          value: "8080"
        volumeMounts:
          - mountPath: /config
            name: somemount

